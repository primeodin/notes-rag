# Git remotes

A remote is a shared copy of your repo, usually on GitHub.
`git push` sends your commits to the remote.
`git pull` brings remote commits into your local branch.
Always `git status` before you push if you are unsure what will leave your machine.
