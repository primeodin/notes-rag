# What RAG is

Retrieval-Augmented Generation finds relevant notes first, then asks a model to answer using those notes.
Cite the note titles you used so the reader can check your work.
If nothing relevant is retrieved, say you do not know instead of inventing.
