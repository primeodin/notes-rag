# Ollama basics

Ollama runs local language models with an OpenAI-compatible API.
Default base URL is often `http://localhost:11434/v1`.
Pull a small model first, then point your CLI at that base URL.
Offline practice still matters: mock modes teach the wiring before tokens cost money.
